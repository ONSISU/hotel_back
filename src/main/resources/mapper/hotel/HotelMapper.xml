<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.hotel_back.hotel.mapper.HotelMapper">

    <select id="getOwnHotelIdList"
            parameterType="long"
            resultType="long">
        SELECT own_hotel_id from own_hotel where hotel_id = #{hotelId};
    </select>


</mapper>
